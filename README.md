# Brewlytic

To run the project locally:

```shell
$ npm install
$ npm mac
```

```shell
$ npm install
$ npm start
$ npm watch
```

If you need to build the project separately from developement:

```shell
$ npm build
```
